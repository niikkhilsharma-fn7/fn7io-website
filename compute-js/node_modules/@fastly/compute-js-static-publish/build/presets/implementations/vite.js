export class VitePreset {
    constructor() {
        this.name = 'Vite';
        this.defaultOptions = {
            'public-dir': './dist',
            name: 'my-vite-app',
            description: 'Compute@Edge static site from Vite',
        };
    }
    check() {
        return true;
    }
}
